version: '3.8'

services:
  drawio:
    # 使用 main 目录下的 Dockerfile 进行构建
    build: ./main
    container_name: drawio-with-auth
    ports:
      - "8080:8080"
      - "8443:8443"
    environment:
      # 在这里设置你的访问密码
      - DRAWIO_ACCESS_CODE=please_change_me
    restart: unless-stopped